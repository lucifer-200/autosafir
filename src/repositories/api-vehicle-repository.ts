import type { VehicleRepository } from "@/repositories/vehicle-repository";

/**
 * Future ASP.NET Core adapter boundary. The demo must use DemoVehicleRepository.
 * Deliberately not implemented until production API contracts are approved.
 */
export type ApiVehicleRepository = VehicleRepository;
